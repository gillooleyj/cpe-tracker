# ── Supabase ──────────────────────────────────────────────────────────────────
# Found in your Supabase project: Settings → API

# Project URL (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Anon / public key (safe to expose to the browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key — SERVER ONLY. Never expose this to the browser.
# Used to delete MFA factors via the admin API when a backup code is redeemed.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Optional ──────────────────────────────────────────────────────────────────

# Session inactivity timeout in minutes (defaults to 15 if not set)
NEXT_PUBLIC_SESSION_TIMEOUT_MINUTES=15

# Beta access gate — blocks all pages behind a password prompt.
# Remove or leave empty to disable the gate (e.g. after user testing).
# Also set this in Vercel → Project Settings → Environment Variables.
SITE_PASSWORD=
